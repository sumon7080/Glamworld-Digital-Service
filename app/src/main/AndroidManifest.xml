<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.glamworlddigitalservice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ItServiceBusiness"></activity>
        <activity android:name=".serviceList.Immigrants" />
        <activity android:name=".NewActivity" />
        <activity android:name=".Software" />
        <activity android:name=".Contact" />
        <activity android:name=".feedback.Feedback" />
        <activity android:name=".serviceList.ServiceList" />
        <activity android:name=".AboutUs" />
        <activity android:name=".serviceList.StudentService" />
        <activity android:name=".Profile" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>